{
  "name": "pager-model",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "test": "mocha -R spec -u tdd test/tests/*.js"
  },
  "dependencies": {
    "lodash": "^2.4.1"
  },
  "devDependencies": {
    "mocha": "~1.12.0",
    "tape": "~1.1.0",
    "tap": "~0.4.4",
    "broadway": "~0.2.7"
  },
  "description": "Generates a pagination model for use in websites."
}
